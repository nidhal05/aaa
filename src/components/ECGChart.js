import React from 'react';
import { View, Text } from 'react-native';

const ECGChart = ({ data }) => {
  // Implémentez votre composant de graphique ECG ici
  return (
    <View>
      <Text>Graphique ECG à implémenter</Text>
    </View>
  );
};

export default ECGChart;